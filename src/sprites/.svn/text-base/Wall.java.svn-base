package sprites;

/**
 * Initialize Wall class, border of maze 
 * @author kd
 *
 */
public class Wall extends Sprite {
	
	/**
	 * Constructor
	 * @param symbol
	 * @param row
	 * @param column
	 */
	public Wall(char symbol, int row, int column){
		super(symbol, row, column);
	}

}
