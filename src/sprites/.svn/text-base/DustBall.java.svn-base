package sprites;

/**
 * A class of Dirt sprite with an associated points value
 * such that 'cleaning' the sprite increases player's score.
 * 
 * Class is only responsible for its constructor, keeping 
 * track of the symbol and points value associated with 
 * any DustBall, as well as the DustBall's position in the 
 * game grid.
 * 
 * @author kd
 *
 */
public class DustBall extends Dirt implements Moveable {
	
	public DustBall(char symbol, int row, int column, int value){
		super(symbol, row, column, value);
	}
		
	public void moveTo(int row, int column){
		this.row = row;
		this.column = column;
	}
}

